<nav class="fixed top-0 left-0 right-0 bg-white shadow">
  <div class="container mx-auto flex justify-between items-center p-4">
    <%= link_to 'Gem Search', root_path, class: 'font-bold' %>
    <div class="flex items-center space-x-4">
      <%= link_to 'Pricing', pricing_path, class: 'text-gray-700 hover:text-blue-600' %>
      <% if user_signed_in? %>
        <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: 'text-gray-700 hover:text-blue-600' %>
      <% else %>
        <%= link_to 'Sign up', new_user_registration_path, class: 'text-gray-700 hover:text-blue-600' %>
        <%= link_to 'Log in', new_user_session_path, class: 'text-gray-700 hover:text-blue-600' %>
      <% end %>
    </div>
  </div>
</nav>

